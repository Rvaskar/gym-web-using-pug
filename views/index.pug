<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <title>This is plain html using pug</title> -->
    <title>#{title}</title>
    style
       include ../static/style.css
    
</head>
<body>
    <nav>
        <ul>
            <li>Home</li>
            <li>About</li>
            <li>Services</li>
            <li>Contact</li>
        </ul>
    </nav>
    <div class="container">
        
        <h2>Get This gym membership for 60$- Fill this form now</h2>
        | <!-- in above line some time there will be some error occurred because of the syntax so we can write it as == -->
        |  <!--     ( | #{content}   )  -->
        <form action="/" method="post" id="contact">
            <input type="text" name="name" id="name" placeholder="Enter your name">
            <input type="text" name="age" id="age" placeholder="Enter your age">
            <input type="text" name="gender" id="gender" placeholder="Enter your gender">
            <textarea name="address" id="address" rows="10" cols="10" placeholder="Enter your address"></textarea>
            <textarea name="more" id="more" rows="10" cols="10" placeholder="Enter more Information about you health...."></textarea>
            <button class="btn">Submit</button>
        </form>
    </div>
</body>
</html>